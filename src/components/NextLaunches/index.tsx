import * as React from 'react';
import NextLaunches from './NextLaunches';

interface Props {
}

const NextLaunchesContainer: React.FC<Props> = () => {

  return <NextLaunches />;
};

export default NextLaunchesContainer;
